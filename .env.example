# Copy to .env and adjust for your MySQL (e.g. Laragon defaults).
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=

DB_NAME=cpd

# Login API (Express). Vite dev proxies /api → this port (see vite.config.ts).
API_PORT=3001
